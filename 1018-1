#include <stdio.h>

int main()
{
    int input,sum=0;
    scanf("%d",&input);
    for (int i = 1; i <= input; i++)
    {
        if (i%3==0)
        {
            sum=sum+i;
        }
    }
    printf("%d\n",sum);
}
