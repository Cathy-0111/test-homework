#include <stdio.h>
#include<math.h>

int main(){
    int n,m,sum=0,i=0,a;
    scanf("%d",&n);
    m=n;
    while(m/10>0){
        sum+=m%10;
        m=m/10;
        i++;
    }
    a=pow(10,i);
    sum=sum+n/a;
    i+=1;
    printf("%d\n",i);
    printf("%d\n",sum);
}
